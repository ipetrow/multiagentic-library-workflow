[project]
name = "multiagentic-library-workflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14.2"
dependencies = [
    "anthropic>=0.112.0",
    "mcp>=1.28.1",
    "python-dotenv>=1.2.2",
]
